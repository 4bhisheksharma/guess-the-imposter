import 'package:flutter/material.dart';

/// App color palette designed for a deduction/party game aesthetic.
class AppColors {
  AppColors._();

  // Primary brand colors
  static const Color primary = Color(0xFF6C5CE7);
  static const Color primaryLight = Color(0xFFA29BFE);
  static const Color secondary = Color(0xFFFF7675);
  static const Color accent = Color(0xFF00CEC9);

  // Background & Surface
  static const Color backgroundDark = Color(0xFF13131A);
  static const Color surfaceDark = Color(0xFF1E1E2D);
  static const Color surfaceElevated = Color(0xFF27273A);

  // Status & Role colors
  static const Color imposter = Color(0xFFE74C3C);
  static const Color civilian = Color(0xFF2ECC71);
  static const Color warning = Color(0xFFF39C12);

  // Text colors
  static const Color textPrimary = Color(0xFFF5F6FA);
  static const Color textSecondary = Color(0xFFB2BEC3);
  static const Color textMuted = Color(0xFF636E72);

  // Borders & Dividers
  static const Color border = Color(0xFF2F3542);
}
